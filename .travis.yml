language: ruby
sudo: false
before_install:
  - gem install bundler
  - gem update bundler
rvm:
  - 2.3.3
gemfile:
  - gemfiles/rails_4.2.gemfile
  - gemfiles/rails_5.0.gemfile
env:
  - BOOKINGSYNC_URL=https://bookingsync.com
script: "bundle exec rake spec"
